Given a positive integer n, print the first n odd natural.
Dado um número inteiro positivo n, imprimir os n primeiros naturais ímpares.

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n, i;
    scanf("%d", &n);
    for (i = 0; i <= n; i++){
        if(i % 2 != 0){
            printf ("%d ", i);
        }
    }
    printf("\nEnd!\n");
    return 0;
}
